{
  "name": "WMPFDebugger",
  "version": "1.0.0",
  "main": "dist-electron/main.js",
  "packageManager": "npm@11.9.0",
  "license": "GPL-2.0-only",
  "author": "evi0s",
  "scripts": {
    "cli": "ts-node src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "test": "npm run typecheck && node --test tests\\pluginPanelLogic.test.cjs tests\\builtinHookTemplates.test.cjs tests\\logsPanelLogic.test.cjs",
    "build": "npm run build:electron",
    "build:electron": "tsc -p tsconfig.electron.json",
    "start": "npm run build:electron && electron .",
    "dev": "npm run build:electron && electron ."
  },
  "dependencies": {
    "frida": "^17.3.2",
    "protobufjs": "^7.5.4",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "electron": "^33.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
